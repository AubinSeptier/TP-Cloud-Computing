# This file defines the Kubernetes Service for the frontend application, which exposes it on port 80.
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80