# This file defines the Kubernetes Service for the db application, which exposes it on port 5432.
apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  selector:
    app: db
  ports:
  - port: 5432
    targetPort: 5432