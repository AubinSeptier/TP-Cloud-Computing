# Docker Compose file for a Flask backend with PostgreSQL and a React frontend
# This file defines the services, networks, and volumes for the application.
version: '3.11'

services:
  db: # PostgreSQL database service
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend: # Flask backend service
    build: ./backend
    restart: always
    depends_on:
      - db
    environment: 
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_ACCESS_TOKEN_EXPIRES=${JWT_ACCESS_TOKEN_EXPIRES}
      - PYTHONUNBUFFERED=1
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    command: >
      sh -c "
        echo 'Waiting for PostgreSQL...'
        while ! pg_isready -h db -p 5432; do
          sleep 1
        done
        echo 'PostgreSQL is ready!'
        if [ ! -d 'migrations' ]; then 
          flask db init
        fi
        flask db migrate
        flask db upgrade
        gunicorn --bind 0.0.0.0:5000 'app:create_app()'"

  frontend: # React frontend service
    build: ./frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"
    environment:
      - REACT_APP_API_URL=https://localhost:5000/api

volumes:
  postgres_data: